# Web-Style-Guide

A web-Style project for converting CSS into Sass by spliting the code into several partial files, and also storing repeated values (length units, colors, etc) in the CSS into Sass variables. Testing.
